# Copy to .env and set values, then: source .env && python3 app.py

FINANCE_DB=finance.db
SECRET_KEY=change-me-to-a-random-string
DEBUG=true
PORT=5000

# Set this to enable password protection (leave empty = no auth)
FINANCE_PASSWORD=
